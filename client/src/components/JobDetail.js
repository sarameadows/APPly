const JobDetail = ({onClose, currentJob}) => {
  const { dateApplied, datePosted, title, company, link, location, officeSetting, pay, source, applicationStatus, requirements, benefits } = currentJob;
  return(
    
  )
}